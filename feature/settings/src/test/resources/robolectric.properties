# Robolectric's API 36+ sandboxes require Java 21; this project is pinned to JDK 17, so tests
# emulate API 35. Irrelevant for SQLite behaviour, which is what these tests exercise.
# Revisit when the project moves to JDK 21 (needed anyway for Compose/widget tests at API 36+).
sdk=35
